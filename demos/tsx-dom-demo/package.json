{
  "name": "tsx-dom-demo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "author": "Santo Pfingsten",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --outdir=www/dist",
    "start": "npm run build -- --watch --servedir=www",
    "test": "tsc"
  },
  "nodemonConfig": {
    "ext": "js,ts,tsx,md,scss,png,webp,svg",
    "ignore": [
      "dist/*"
    ]
  },
  "dependencies": {
    "esbuild": "^0.21.4",
    "tsx-dom": "^2.0.0"
  }
}
